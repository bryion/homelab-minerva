all:
  children:
    k3s:
      hosts:
        minerva:
          ansible_host: 192.168.1.X  # Replace with node IP
          ansible_user: ubuntu
          ansible_become: true
          ansible_python_interpreter: /usr/bin/python3
