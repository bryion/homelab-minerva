# This directory is managed by `flux bootstrap github`.
# Do not manually edit files here — Flux will overwrite them on reconciliation.
# Run: flux bootstrap github --owner=<org> --repository=homelab --branch=main --path=kubernetes
